import React from 'react';

const Home = () => (
  <main className="app-main">
    <h1>React HMR with custom Express server</h1>
    <p>React HMR with custom Express server is up and running!</p>
  </main>
);

export default Home;
